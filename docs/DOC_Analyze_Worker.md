# Попытка анализа сервиса

Кому может быть полезен, сколько займет времени выполнение схемы, какие узкие места могут возникнуть и как их решать.
Забегая вперед, основа: postgresql+rabbitmq+worker на go.

## Сценарий 1: Компания по аренде машин (500 машин, 6-7 схем по 20-30 шагов)

### Математика производительности

**Допустим:**
- 500 машин
- В среднем каждая машина генерирует 10 событий в день (старт аренды, окончание, проверки сенсоров)
- = **5000 executions/день** или **~3.5 executions/минуту**
- Каждая схема: 25 шагов в среднем

**Нагрузка на систему:**
- 3.5 executions/мин × 25 шагов = **87.5 сообщений/минуту** в RabbitMQ
- Это **1.5 сообщения/секунду** 

**Latency одного шага:**
- RabbitMQ roundtrip: ~5-10мс (локально) или ~20-50мс (сеть)
- Выполнение ноды: 10-100мс (зависит от типа)
- Сохранение в БД: 5-20мс
- **Итого на шаг: ~50-150мс**

**Полная схема из 25 шагов:**
- 25 × 100мс (среднее) = **2.5 секунды**
- С учётом HTTP запросов (если есть) может вырасти до **5-10 секунд**

### Узкие места ❌

1. **Последовательное выполнение** - каждый шаг ждёт предыдущий
   - 25 шагов × 100мс = 2.5 сек минимум
   - Если в схеме есть HTTP запросы по 500мс → 25 секунд!

2. **Overhead RabbitMQ** - каждый шаг = отдельное сообщение
   - Для "горячих" сценариев (проверка сенсора каждые 10 сек) это может быть проблемой

3. **Context растёт** - после каждого шага context увеличивается
   - Сохранение 100KB JSON в БД каждый шаг → медленно

### Решения ✅

**1. Hybrid батчинг** (из нашего обсуждения):
```go
const (
    MaxNodesPerBatch = 10       // выполнить 10 нод подряд
    MaxBatchDuration = 30*time.Second
)
```
- Быстрые ноды (Log, Variable Set) выполняются пачкой без RabbitMQ
- После 10 нод или 30 сек → re-publish
- **Улучшение: 25 шагов за ~500мс вместо 2.5 сек** ⚡

**2. Асинхронные ноды:**
- HTTP Request с большим timeout → вынести в отдельную ноду
- Вместо ожидания 5 секунд внутри схемы:
  ```
  Start → Webhook отправка → End
  (внешний сервис ответит через webhook → запустит другую схему)
  ```

**3. Кэширование context:**
- Использовать Redis для execution_state вместо PostgreSQL
- PostgreSQL только для execution_steps (история)
- **Ускорение read/write: 1-2мс вместо 10-20мс**

### Плюсы для компании по аренде 🎉

1. **Единый центр логики** - не размазано по коду
2. **Лёгкая модификация** - добавил проверку → перетащил ноду
3. **Аудит** - вся история в execution_steps
4. **A/B тесты** - два варианта схемы параллельно
5. **Без даунтайма** - обновил схему → сразу работает
6. **Визуальный дебаг** - видно где упало

**Вердикт:** Для 500 машин система легко справится даже без оптимизаций. С батчингом будет вообще незаметно.

---

## Сценарий 2: Обычный человек (почта, сервис, обед)

### Сценарий использования

**Схемы:**
1. Утром: Проверить почту Gmail → если важное письмо → отправить в Slack
2. В 12:00: Отправить сообщение в Telegram другу "Пошли обедать!"
3. Вечером: Скачать отчёты с сервиса → отправить себе на email

**Частота:**
- 3 схемы в день × 5-10 шагов = **15-30 executions/день**
- Один пользователь генерирует **пренебрежимо малую** нагрузку

**Если таких пользователей 10,000:**
- 10,000 × 30 = **300,000 executions/день**
- = **3.5 executions/секунду**
- × 8 шагов в среднем = **28 сообщений/секунду**

**Одна машина воркера:**
- Может обрабатывать **100-500 сообщений/секунду** (зависит от нагрузки нод)
- **Запас: 3-10x** ✅

### Узкие места ❌

1. **Scheduler триггеры** - если все 10K пользователей запустят схему в 12:00
   - Burst: 10,000 executions одновременно
   - RabbitMQ справится (он для этого и создан)
   - Но воркеры могут не успеть → задержка 1-2 минуты

2. **HTTP запросы к внешним API:**
   - Gmail API: 250 requests/second (лимит)
   - Telegram API: 30 requests/second per bot
   - **Нужен rate limiter!**

3. **Долгие ноды:**
   - Скачать 10MB файла → 5-10 секунд
   - Пока воркер ждёт → не обрабатывает другие executions
   - **Решение:** асинхронные ноды или больше воркеров

### Решения ✅

**1. Rate Limiting на воркере:**
```go
rateLimiter := rate.NewLimiter(rate.Limit(20), 50) // 20 req/sec, burst 50
rateLimiter.Wait(ctx) // перед каждым HTTP запросом
```

**2. Приоритизация очередей:**
- `high_priority_queue` - для критичных схем
- `low_priority_queue` - для рутинных
- Воркеры берут из high с приоритетом

**3. Масштабирование:**
- 1 воркер → 10,000 пользователей ✅
- 10 воркеров → 100,000 пользователей ✅
- Просто запустить больше инстансов

### Плюсы для обычного человека 🎉

1. **Автоматизация рутины** - забыл про ручную работу
2. **Визуальный редактор** - не нужно код писать
3. **История** - видно что и когда выполнилось
4. **Простота изменений** - перетащил блок → готово
5. **Бесплатно и self-hosted** - без зависимости от Zapier/IFTTT

**Вердикт:** Система отлично подходит для таких сценариев. Это её основная аудитория!

---

## Сценарий 3: Школьник/студент (алгоритм сортировки, 100 шагов)

### ⚠️ Проблема: AlgoMap НЕ для этого!

**Почему:**
- Каждый шаг = сообщение RabbitMQ + запись в БД
- 100 шагов × 100мс = **10 секунд минимум**
- Реальная сортировка на CPU: **0.001 секунды** (в 10,000 раз быстрее!)

**Что происходит:**
```
Bubble Sort: 100 элементов → ~5000 сравнений

В AlgoMap:
- 5000 нод "Compare"
- 5000 сообщений в RabbitMQ
- 5000 записей в execution_steps
- Время выполнения: 5000 × 100мс = 8 минут 😱

В Python:
- 5000 операций на CPU
- Время: 0.01 секунды
```

### Узкие места ❌

1. **Overhead на каждую операцию** - сохранение в БД, RabbitMQ
2. **Низкоуровневые операции** - сравнение двух чисел не должно быть нодой
3. **Память** - context вырастает до мегабайтов

### Можно ли использовать? 🤔

**ДА, но для "высокоуровневых" алгоритмов:**

❌ **НЕ подходит:**
- Bubble Sort (5000 сравнений)
- Quick Sort (много мелких операций)
- Matrix multiplication (миллионы операций)

✅ **Подходит:**
- **ETL pipelines:** Load CSV → Transform → Save to DB (10-20 шагов)
- **Обработка данных:** Fetch API → Parse → Filter → Aggregate → Send (15 шагов)
- **Бизнес-процессы:** Заявка → Проверка → Одобрение → Уведомление (10 шагов)

**Пример "правильного" использования:**
```
Алгоритм анализа данных студентов:
1. Fetch data from Google Sheets (1 нода)
2. Parse CSV (1 нода с python script внутри)
3. Calculate average (1 нода)
4. Generate chart (1 нода)
5. Send to Telegram (1 нода)

Итого: 5 нод = 0.5 секунды выполнения ✅
```

### Решение для "алгоритмических" задач

**Добавить ноду "Code Execution":**
```json
{
  "type": "code_execution",
  "config": {
    "language": "python",
    "code": "def bubble_sort(arr): ...",
    "input": "{{variables.array}}",
    "timeout": 30
  }
}
```

Воркер запускает код в sandbox (Docker container) и возвращает результат.

**Тогда:**
- 1 нода "Code Execution" выполнит весь Bubble Sort за 0.01 секунды
- AlgoMap используется для **оркестрации**, а не для каждой операции

### Плюсы для студента 🎉

1. **Визуализация высокоуровневой логики** - не детали алгоритма, а общий flow
2. **Обучение оркестрации** - как системы взаимодействуют
3. **Прототипирование** - быстро собрать MVP data pipeline
4. **Дебаг** - видно где упало в сложном процессе

**Вердикт:** Для обучения классическим алгоритмам (сортировки, поиск) - не подходит. Для обучения системному дизайну и data pipelines - отлично!

---

## Общие узкие места системы

### 1. **PostgreSQL как bottleneck**

**Проблема:**
- При 1000 executions/sec нужно писать 1000 × 25 = **25,000 rows/sec** в execution_steps
- PostgreSQL может ~10,000 writes/sec на SSD

**Решение:**
- Batch inserts (вставлять по 100 rows за раз)
- Партиционирование execution_steps по дате
- Использовать TimescaleDB вместо обычного PostgreSQL

### 2. **Context размер**

**Проблема:**
- После 100 шагов context может вырасти до 1-5 MB
- Сохранение 5MB JSONB каждый шаг = медленно

**Решение:**
- Limit на размер context (например, 1MB)
- Старые steps.*.output автоматически удаляются из context
- Хранятся только в execution_steps (история)

### 3. **RabbitMQ как SPOF**

**Проблема:**
- Если RabbitMQ упадёт → вся система встанет

**Решение:**
- RabbitMQ cluster (3 ноды)
- Persistence включена (сообщения на диск)
- Auto-reconnect в воркере

### 4. **Холодный старт воркеров**

**Проблема:**
- При burst нагрузке воркеры не успевают

**Решение:**
- Auto-scaling (Kubernetes HPA)
- Держать минимум 3-5 воркеров всегда
- Warm pool: воркеры уже подключены к БД/RabbitMQ

---

## Цифры: сколько система может?

### Conservative (без оптимизаций):
- **1 воркер:** 10-50 executions/sec
- **10 воркеров:** 100-500 executions/sec
- **100 воркеров:** 1,000-5,000 executions/sec

### Optimized (с батчингом + Redis):
- **1 воркер:** 50-200 executions/sec
- **10 воркеров:** 500-2,000 executions/sec
- **100 воркеров:** 5,000-20,000 executions/sec

### Это покрывает:
- **1,000 executions/sec** = 86 миллионов executions/день
- = 1 миллион активных пользователей (если каждый 86 executions/день)

---

## Итоговая таблица: "Подходит ли AlgoMap?"

| Сценарий | Executions/день | Шагов | Latency | Подходит? | Примечания |
|----------|----------------|-------|---------|-----------|------------|
| Аренда машин (500 машин) | 5,000 | 20-30 | 2-5 сек | ✅ Отлично | С батчингом 0.5-1 сек |
| Личная автоматизация (10K users) | 300,000 | 5-10 | 0.5-2 сек | ✅ Отлично | Основной use case |
| Алгоритм сортировки | 1 | 5,000 | 8 мин | ❌ Плохо | Использовать Code Execution ноду |
| ETL pipeline студента | 100 | 10-15 | 1-3 сек | ✅ Хорошо | Для обучения идеально |
| E-commerce (100K orders/day) | 100,000 | 30-50 | 3-8 сек | ✅ Хорошо | Нужно 5-10 воркеров |

---

## Выводы

1. **AlgoMap — это оркестратор**, а не вычислитель
2. Для бизнес-процессов и автоматизации — **идеально** ✅
3. Для низкоуровневых алгоритмов — **не подходит** ❌
4. Latency 0.5-5 сек на схему — это **нормально** для большинства задач
5. Масштабируется до миллионов executions/день легко 🚀

**Самое узкое место:** PostgreSQL при записи истории. Решается партиционированием и batch inserts.

Что думаешь? Может что-то ещё проверим? 🤔