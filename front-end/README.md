# AlgoMap Frontend

Визуальный редактор блок-схем на React + React Flow.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск dev-сервера

```bash
npm run dev
```

Приложение откроется на `http://localhost:3000`

### Сборка для продакшена

```bash
npm run build
```

## 🏗️ Структура проекта

```
src/
├── api/              # API клиент (axios)
│   └── client.js
├── components/       # Переиспользуемые компоненты
│   ├── CustomNode.jsx
│   ├── NodesPalette.jsx
│   └── NodeConfigPanel.jsx
├── pages/            # Страницы
│   ├── AuthPage.jsx
│   ├── DashboardPage.jsx
│   └── EditorPage.jsx
├── store/            # Zustand stores
│   ├── authStore.js
│   └── schemasStore.js
├── styles/           # CSS файлы
│   ├── App.css
│   ├── Auth.css
│   ├── Dashboard.css
│   └── Editor.css
├── utils/            # Утилиты
│   └── nodeTypes.js
├── App.jsx
└── main.jsx
```

## 🎨 Возможности

- ✅ Аутентификация (Login/Register)
- ✅ CRUD схем
- ✅ Визуальный редактор с drag & drop
- ✅ Панель инструментов с типами нод
- ✅ Настройка параметров нод
- ✅ Автосохранение изменений
- ✅ React Flow интеграция

## 🔧 Настройка

### API Backend

По умолчанию frontend проксирует запросы к `http://localhost:8080/api`.

Для изменения настроек отредактируйте `vite.config.js`:

```javascript
server: {
  proxy: {
    '/api': {
      target: 'http://your-backend-url:port',
      changeOrigin: true,
    }
  }
}
```

## 📦 Зависимости

- **react** - UI библиотека
- **react-router-dom** - Роутинг
- **reactflow** - Визуальный редактор графов
- **axios** - HTTP клиент
- **zustand** - Стейт-менеджмент
- **vite** - Сборщик и dev-сервер

## 🎯 Типы нод

### Управление потоком
- Start ▶️
- End ⏹️
- Condition 🔀

### Данные
- Set Variable 💾
- Math 🔢

### Внешние вызовы
- HTTP Request 🌐
- RabbitMQ 🐰

### Логика
- Log 📝

### Время
- Sleep ⏰

## 🛠️ Разработка

### Добавление нового типа ноды

1. Добавьте определение в `src/utils/nodeTypes.js`:

```javascript
[NODE_TYPES.YOUR_NODE]: {
  type: NODE_TYPES.YOUR_NODE,
  label: 'Your Node',
  category: NODE_CATEGORIES.LOGIC,
  icon: '🎯',
  color: '#3b82f6',
  outputs: 1,
  config: {
    // default config
  },
}
```

2. Добавьте форму настройки в `src/components/NodeConfigPanel.jsx`

3. Стили автоматически применятся из `src/styles/Editor.css`

### Структура данных схемы

```javascript
{
  "name": "My Schema",
  "description": "Description",
  "definition": {
    "nodes": [
      {
        "id": "start_1",
        "type": "custom",
        "position": { "x": 100, "y": 100 },
        "data": {
          "type": "start",
          "label": "Start",
          "config": {}
        }
      }
    ],
    "edges": [
      {
        "id": "edge_1",
        "source": "start_1",
        "target": "log_1"
      }
    ]
  }
}
```

## 🔐 Авторизация

Session-based auth с Bearer токеном:

1. Login → получаем `session_key`
2. Сохраняем в `localStorage`
3. Каждый запрос содержит `Authorization: Bearer {session_key}`
4. При 401 - редирект на `/login`

## 🐛 Известные проблемы

- Нет валидации схем (будет добавлено)
- Нет истории изменений (undo/redo)
- Нет экспорта/импорта JSON

## 📝 TODO

- [ ] Валидация схем перед сохранением
- [ ] Undo/Redo
- [ ] Zoom controls
- [ ] Копирование/вставка нод
- [ ] Экспорт в PNG
- [ ] Dark mode
- [ ] i18n (EN/RU)
- [ ] Тесты

## 📄 Лицензия

MIT
